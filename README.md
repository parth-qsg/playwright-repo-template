# Playwright repo template

This repository is a minimal, compatible Playwright project.

It is designed so:
- script generation writes files into `tests/`
- execution runs those files with `npx playwright test`
- credentials and URL come from environment variables expected by runner allowlists

